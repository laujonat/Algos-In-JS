{
  "name": "algs_in_js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "eslint-plugin-ecmascript-compat": "^1.0.0",
    "eslint-plugin-flowtype": "^4.7.0",
    "flow": "^0.2.3",
    "flow-typed": "^3.1.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.0",
    "@babel/preset-flow": "^7.9.0",
    "flow-bin": "^0.122.0",
    "flow-remove-types": "^2.122.0"
  },
  "scripts": {
    "start": "npm run build && node index.js",
    "build": "npm run flow:build && npm run build:ds && npm run build:problems && npm run build:sandbox",
    "build:ds": "babel ./datastructures/ -d lib/",
    "build:problems": "babel ./problems/ -d lib/",
    "build:sandbox": "babel ./sandbox/ -d lib/",
    "flow:problems": "flow-remove-types ./problems/ -d ./lib/ --all --pretty",
    "flow:ds": "flow-remove-types ./datastructures/ -d ./lib/ --all --pretty",
    "flow:sandbox": "flow-remove-types ./sandbox/ -d ./lib/ --all --pretty",
    "flow:build": "npm run flow:ds && npm run flow:problems && npm run flow:sandbox",
    "test": "echo \"Error: no test specified\" && exit 1",
    "flow": "flow"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}