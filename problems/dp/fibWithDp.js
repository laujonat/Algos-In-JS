/*
  def fib(n)
    if n == 0 or n == 1
      return n
    else 
      return fib(n-1) + fib(n-2)

  memo = {}
   def fib(n
    if(memo[n])
      return memo[n]
    if n == 0 or n == 1
      return n
    else 
      memo[n] = fib(n-1) + fib(n-2)
*/

module.exports = {};
